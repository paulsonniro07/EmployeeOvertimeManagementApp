@{
    ViewBag.Title = "Main Menu";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row">
    <div class="col-md-6">
        <div class="box box-primary">
            <div class="box-header with-border">
                <h3 class="box-title">Main Menu</h3>
            </div>
            <div class="box-body">
                <div class="row">
                    <div class="col-md-6">
                        <a href="@Url.Action("Index", "Employees")" class="btn btn-app">
                            <i class="fa fa-users"></i> Employee
                        </a>
                    </div>
                    <div class="col-md-6">
                        <a href="@Url.Action("Index", "Overtimes")" class="btn btn-app">
                            <i class="fa fa-clock-o"></i> Over Time Entry
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>